{
  "name": "instead",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "cd backend && node dist/server.js",
    "build": "cd client && yarn install && yarn run build && cd ../backend && yarn install && yarn run build",
    "local": "concurrently \"cd backend && yarn run watch-debug\" \"cd client && yarn start\""
  },
  "keywords": [],
  "author": "Daniel Brauer",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/danielbrauer/instead.git"
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^4.1.2"
  }
}
