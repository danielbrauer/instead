{
  "name": "instead",
  "version": "0.1.0",
  "engines": {
    "node": "12.13.1",
    "yarn": "1.22.4"
  },
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node backend/dist/server.js",
    "build": "cd backend && yarn install && yarn build && cd ../client && yarn install && yarn build",
    "local": "concurrently \"cd backend && yarn watch-debug\" \"cd client && yarn start\""
  },
  "keywords": [],
  "author": "Daniel Brauer",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/danielbrauer/instead.git"
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^5.2.0"
  }
}
